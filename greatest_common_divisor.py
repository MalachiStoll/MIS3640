def gcf(a,b)
    if a > b:
        smaller variable = b
    else: 
         smaller variable = b
    for i in range(smaller variable+1):
        if ((a % == 0) and (b % == 0)):
        gcf = i 
    return gcf

